<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical" >

    <Button android:id="@+id/btnLinearLayoutHorizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="LinearLayout Horizontal"
        android:onClick="linearLayoutHorizontal"/>

    <Button android:id="@+id/btnLinearLayoutVertical"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="LinearLayout Vertical"
        android:onClick="linearLayoutVertical"/>

    <Button android:id="@+id/btnRelativeLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="RelativeLayout"
        android:onClick="relativeLayout"/>

    <Button android:id="@+id/btnTableLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="TableLayout"
        android:onClick="tableLayout"/>

    <Button android:id="@+id/btnFrameLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="FrameLayout"
        android:onClick="frameLayout"/>

</LinearLayout>